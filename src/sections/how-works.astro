---
import IconArrow from "../components/icon-arrow.astro"
---

<section class="how-works">
	<img class="mx-auto" src="/images/illustration-stay-productive.png" alt="people working" />
	<div class="how-works--info max-w-prose">
		<h2 class="fs-500">Stay productive, wherever you are</h2>

		<p>
			Never let location be an issue when accessing your files. Fylo has you covered for all of your
			file storage needs.
		</p>

		<p>
			Securely share files and folders with friends, family and colleagues for live collaboration.
			No email attachments required.
		</p>

		<a class="fs-300 | how-works--link" href="#">See how Fylo works <IconArrow /></a>
	</div>
</section>

<style>
	.how-works {
		display: grid;
		gap: var(--sp-8);
		margin-bottom: var(--sp-28);
	}

	.how-works--info > * + * {
		margin-top: var(--sp-4);
	}

	.how-works--link {
		--color: hsl(var(--clr-accent-1));
		display: flex;
		align-items: center;
		gap: var(--sp-2);
		color: var(--color);
		border-bottom: 1px solid var(--color);
		padding-bottom: var(--sp-1);
		max-width: fit-content;
	}

	.how-works--link:hover,
	.how-works--link:focus-within {
		--color: currentColor;
	}

	@media (min-width: 48em) {
		.how-works {
			grid-template-columns: 55% 1fr;
			place-items: center;
		}

		h2 {
			font-size: var(--fs-900);
		}

		a {
			font-size: var(--fs-500);
		}
	}
</style>
